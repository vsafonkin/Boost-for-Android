name: Build Boost

on: [push]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run script
      run: |
        sudo chmod +x ./run.sh
        sudo ./run.sh
